# WEARME — Body parameters configuration
# Populated in Phase 1 (Corps manuel MVP)

# Parameter limits will be defined here as:
#   <param_name>:
#     min: <float>   # minimum value (meters or dimensionless)
#     max: <float>   # maximum value
#     default: <float>
#     unit: <str>    # "m" | "cm" | "kg" | "dimensionless"
#     description: <str>

# Example (placeholder — Phase 1 will set real values):
# height:
#   min: 1.40
#   max: 2.20
#   default: 1.75
#   unit: m
#   description: Total standing height

# TODO Phase 1: fill in all SMPL shape parameters + derived measurements
